# VCS
.git
.gitignore

# IDE / editor
.idea
*.swp

# Docs (not needed in the image)
*.md
README.md

# Node
node_modules
dist
build
*.tsbuildinfo
npm-debug.log*

# Generated UI output + vendored assets (rebuilt inside the image)
public

# Data / OSM extracts (mounted as a volume at runtime)
data
*.osm.pbf
*.osm.bz2
*.mbtiles

# Env / secrets
.env
.env.*
!.env.example

# OS / logs
.DS_Store
logs
*.log
